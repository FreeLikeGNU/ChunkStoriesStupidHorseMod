// CHANGE THIS TO SUIT YOUR MOD
rootProject.name = "My're le first mode"

// Comment-out these two lines to de-activate the template plugin
include rootProject.name + '-plugin' 
project(":"+rootProject.name + '-plugin').projectDir = file("plugin")